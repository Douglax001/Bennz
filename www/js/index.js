function toggleMenu() {
    // Seleciona o menu pelo seletor da classe
    const menu = document.querySelector('.menu');

    // Alterna a classe "show" no menu
    menu.classList.toggle('show');
}

function toggleMenu() {
    console.log('Menu button clicked!');  // Adicionado para verificar se a função está sendo executada
    const menu = document.querySelector('.menu');
    menu.classList.toggle('show');
}

